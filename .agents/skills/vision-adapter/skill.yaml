# skill.yaml — manifiesto machine-readable (contrato B2). Validado por scripts/skill-lint.sh.
id: vision-adapter
name: vision-adapter
version: 1.0.0
description: "Pipeline imagen → VLM (kimi_vision.js + see.sh): análisis de capturas y screenshots mediante la API hosted de Hugging Face (HF_TOKEN)."
entry: SKILL.md
safe: true
requires_sudo: false
platforms: [linux, android]
triggers:
  - vision
  - vlm
  - imagen
  - kimi
  - screenshot
capabilities:
  - vision
  - vlm
dependencies: []
